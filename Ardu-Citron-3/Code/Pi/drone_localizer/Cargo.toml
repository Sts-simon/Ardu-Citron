[package]
name = "drone_localizer"
version = "0.1.0"
edition = "2021"

[dependencies]
opencv = { version = "0.92", default-features = false, features = ["calib3d", "imgcodecs", "imgproc", "aruco", "video"] }
image = "0.25"
glob = "0.3" # Ajoute cette ligne si elle n'y est pas
